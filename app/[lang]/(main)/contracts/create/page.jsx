import ContractsCreateForm from '../../../../components/Main/Contracts/ContractsCreateForm';

export default function ContractsCreatePage({ params: { lang } }) {
    return <ContractsCreateForm lang={lang} />;
}
